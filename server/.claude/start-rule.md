---
name: startup-rule
description: How to start this project
type: project
---

# Startup Rule

**When user says "start the project" / "启动当前项目":**
- Run: `go run main.go`
- **DO NOT**: run `build.sh` or `make`

This is for local dev, dependencies are already installed.
